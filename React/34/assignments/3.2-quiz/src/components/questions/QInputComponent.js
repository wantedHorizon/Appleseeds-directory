
import React from 'react';


const quizInputComponent = (props) =>  <input type={props.type} min={props.min} max={props.max} name={props.name} className={props.class} /> ;


export default quizInputComponent;